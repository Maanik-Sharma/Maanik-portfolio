/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",  // Uncomment this line if you need static export
    images: {
      domains: ['images.unsplash.com'], // Add other domains as needed
    },
  };
  
  module.exports = nextConfig;
  